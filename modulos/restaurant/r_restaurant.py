




# router = APIRouter(tags=['Restaurant'])

# @router.get("/api/platillos")
# async def authenticate_User(restaurant_id:str, response: Response, db: Session = Depends(database.get_db)):

#     return jsonable_encoder({'respuesta': 'Usuario y/o contraseña incorrectos'}, sort_keys=True, indent=4)